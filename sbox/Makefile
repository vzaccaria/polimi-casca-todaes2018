# Makefile for sbox. Automatically generated by SBV. Do not edit!

# include any user-defined .mk file in the current directory.
-include *.mk

CC=gcc
CCFLAGS?=-Wall -O3 -DNDEBUG -fomit-frame-pointer

all: sbox_driver

sbox.o: sbox.c sbox.h
	${CC} ${CCFLAGS} -c $< -o $@

sbox_driver.o: sbox_driver.c
	${CC} ${CCFLAGS} -c $< -o $@

sbox_driver: sbox.o sbox_driver.o
	${CC} ${CCFLAGS} $^ -o $@

clean:
	rm -f *.o

veryclean: clean
	rm -f sbox_driver
